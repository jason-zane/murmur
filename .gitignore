# macOS build
.build/
*.xcuserdatad
.DS_Store

# Built .app bundles, at the repo root only.
/*.app/

artifacts/
output/playwright/
.playwright-cli/

# Local ASR model weights at the repo root. Anchoring keeps the app's Models source
# directory visible to Git on case-insensitive macOS filesystems.
/models/
*.onnx
*.nemo

# Cloud dependencies, build artifacts and credentials.
node_modules/
.next/
.vercel/
.env*
!.env.example
*.tsbuildinfo
supabase/.temp/
